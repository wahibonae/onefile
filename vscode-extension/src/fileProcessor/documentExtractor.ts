/**
 * Phase 1 stub — document extraction is implemented in Phase 2.
 * This export exists so fileProcessor/index.ts compiles during Phase 1.
 */
export async function extractDocument(_absolutePath: string): Promise<string> {
  throw new Error('Document extraction not yet supported')
}
